<% provide(:title, "Add Opportunity") %>

<h1>Add Opportunity</h1>
<div class="row">
	<div class="span6 offset3">
		<%= simple_form_for(@opportunity) do |f| %>
			<%= render 'fields', f: f %>
			<%= f.button :submit, "Add opportunity", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>